'use client'

import { useState } from 'react'
import {
  Activity, AlertTriangle, BarChart3, BookOpen, Check, CheckCircle2, ChevronDown, ChevronRight, CircleHelp, ClipboardCheck, FileText, Gauge, LayoutDashboard, LifeBuoy, Menu, MessageSquare, MoreHorizontal, Paperclip, Plus, Search, Send, Settings, ShieldCheck, Sparkles, Upload, UserRound, X, Zap,
} from 'lucide-react'

type Section = 'Dashboard' | 'Documents' | 'AI Assistant' | 'Evaluations' | 'Safeguards' | 'Settings'

const navItems: { label: Section; icon: typeof LayoutDashboard }[] = [
  { label: 'Dashboard', icon: LayoutDashboard }, { label: 'Documents', icon: FileText }, { label: 'AI Assistant', icon: MessageSquare }, { label: 'Evaluations', icon: ClipboardCheck }, { label: 'Safeguards', icon: ShieldCheck }, { label: 'Settings', icon: Settings },
]

const documents = [
  { name: 'Employee Travel Policy.pdf', type: 'PDF', pages: 18, status: 'Indexed', date: 'Today, 09:42', tone: 'success' },
  { name: 'Data Protection Policy.pdf', type: 'PDF', pages: 24, status: 'Indexed', date: 'Yesterday, 16:18', tone: 'success' },
  { name: 'Information Security Policy.pdf', type: 'PDF', pages: 31, status: 'Processing', date: 'Aug 26, 11:03', tone: 'warning' },
  { name: 'Expense Reimbursement Policy.pdf', type: 'PDF', pages: 12, status: 'Needs review', date: 'Aug 24, 14:30', tone: 'danger' },
]

const evalRows = [
  ['Baseline LLM', '82.4%', '13.6%', '4.0%', 'High risk'], ['RAG', '88.8%', '7.2%', '4.0%', 'Improving'], ['RAG + Evidence Threshold', '86.0%', '2.4%', '11.6%', 'Safe'], ['RAG + Verification', '87.2%', '1.6%', '11.2%', 'Strongest'],
]

function StatusPill({ children, tone = 'success' }: { children: React.ReactNode; tone?: 'success' | 'warning' | 'danger' | 'neutral' }) {
  return <span className={`status-pill ${tone}`}><span className="status-dot" />{children}</span>
}

function StatCard({ label, value, change, icon: Icon, tone }: { label: string; value: string; change: string; icon: typeof FileText; tone: string }) {
  return <div className="stat-card"><div className="stat-top"><span className={`icon-box ${tone}`}><Icon size={17} /></span><span className="stat-change">{change}</span></div><div className="stat-value">{value}</div><div className="stat-label">{label}</div></div>
}

function Sidebar({ active, setActive }: { active: Section; setActive: (section: Section) => void }) {
  return <aside className="sidebar"><div className="brand"><div className="brand-mark"><ShieldCheck size={19} /></div><div><strong>Compli<span>Sure</span></strong><small>AI safety workspace</small></div></div><div className="workspace"><span className="workspace-avatar">CG</span><div><small>Workspace</small><b>University AI Lab</b></div><ChevronDown size={15} /></div><nav className="nav-list" aria-label="Main navigation">{navItems.map(({ label, icon: Icon }) => <button key={label} className={`nav-item ${active === label ? 'active' : ''}`} onClick={() => setActive(label)}><Icon size={18} /><span>{label}</span>{label === 'Documents' && <span className="nav-count">12</span>}</button>)}</nav><div className="sidebar-bottom"><div className="safety-mini"><div className="safety-icon"><Zap size={15} /></div><div><b>AI Safety Mode</b><small><span className="pulse-dot" />Active</small></div><ChevronRight size={15} /></div><div className="profile"><span className="profile-avatar">AM</span><div><b>Alex Morgan</b><small>Researcher</small></div><MoreHorizontal size={18} /></div></div></aside>
}

function Topbar({ active, onMenu }: { active: Section; onMenu: () => void }) { return <header className="topbar"><button className="mobile-menu" onClick={onMenu} aria-label="Open menu"><Menu size={21} /></button><div className="breadcrumbs"><span>Workspace</span><ChevronRight size={14} /><b>{active}</b></div><div className="top-actions"><button className="icon-button" aria-label="Help"><CircleHelp size={18} /></button><button className="icon-button" aria-label="Notifications"><span className="notification-dot" /><Activity size={18} /></button><div className="top-divider" /><span className="top-avatar">AM</span></div></header> }

function Dashboard() { return <div className="page-content"><div className="page-heading"><div><div className="eyebrow"><span className="eyebrow-line" />OVERVIEW</div><h1>Compliance Overview</h1><p>Monitor document coverage, answer reliability, and AI safety.</p></div><button className="secondary-button"><BarChart3 size={16} />Export report</button></div><div className="stat-grid"><StatCard label="Documents indexed" value="12" change="+2 this month" icon={FileText} tone="blue" /><StatCard label="Questions tested" value="250" change="+18% vs last cycle" icon={MessageSquare} tone="purple" /><StatCard label="Correct answers" value="218" change="87.2% accuracy" icon={CheckCircle2} tone="green" /><StatCard label="Confident wrong answers" value="8" change="↓ 42% with safeguards" icon={AlertTriangle} tone="orange" /></div><div className="dashboard-grid"><section className="panel reliability-panel"><div className="panel-header"><div><h2>Answer Reliability</h2><p>Evaluation results across 250 test questions</p></div><button className="select-button">Last 30 days <ChevronDown size={14} /></button></div><div className="chart-legend"><span><i className="legend-line correct" />Correct answers</span><span><i className="legend-line refusal" />Correct refusals</span><span><i className="legend-line wrong" />Wrong answers</span></div><div className="line-chart"><div className="y-axis"><span>100%</span><span>75%</span><span>50%</span><span>25%</span><span>0%</span></div><div className="chart-area"><div className="grid-lines"><i /><i /><i /><i /><i /></div><svg viewBox="0 0 720 220" preserveAspectRatio="none" role="img" aria-label="Answer reliability line chart"><path className="chart-fill" d="M0,88 C60,70 90,94 140,65 S220,85 275,56 S350,68 405,47 S490,57 540,35 S625,48 720,22 L720,220 L0,220Z" /><path className="chart-path correct-path" d="M0,88 C60,70 90,94 140,65 S220,85 275,56 S350,68 405,47 S490,57 540,35 S625,48 720,22" /><path className="chart-path refusal-path" d="M0,181 C65,171 90,183 145,164 S235,177 285,151 S360,161 420,138 S500,151 555,128 S635,138 720,112" /><path className="chart-path wrong-path" d="M0,198 C65,195 95,202 150,190 S235,197 285,185 S365,190 425,175 S500,183 555,166 S640,178 720,152" /></svg><div className="x-axis"><span>Aug 01</span><span>Aug 08</span><span>Aug 15</span><span>Aug 22</span><span>Aug 29</span></div></div></div></section><section className="panel safeguard-panel"><div className="panel-header"><div><h2>Safeguard Performance</h2><p>Impact by configuration</p></div><button className="icon-button"><MoreHorizontal size={18} /></button></div><div className="bar-chart">{[['Baseline LLM', 42, 'muted'], ['RAG', 57, 'blue'], ['Evidence threshold', 78, 'green'], ['Verification', 88, 'navy']].map(([label, value, tone]) => <div className="bar-row" key={label as string}><div className="bar-label"><span>{label}</span><b>{value}%</b></div><div className="bar-track"><div className={`bar-fill ${tone}`} style={{ width: `${value}%` }} /></div></div>)}</div><div className="panel-foot"><span className="foot-check"><Check size={14} />4 safeguards active</span><button className="text-button">View details <ChevronRight size={14} /></button></div></section></div><section className="insight-banner"><div className="insight-mark"><Sparkles size={18} /></div><div><b>Safety is improving</b><p>Evidence thresholds reduced confident wrong answers by <strong>42%</strong> in the latest evaluation cycle.</p></div><button className="text-button">See evaluation results <ChevronRight size={14} /></button></section></div> }

function Documents({ notify, setEvidence }: { notify: (message: string) => void; setEvidence: (open: boolean) => void }) { return <div className="page-content"><div className="page-heading"><div><div className="eyebrow"><span className="eyebrow-line" />KNOWLEDGE BASE</div><h1>Compliance Documents</h1><p>Manage the approved sources your assistant is allowed to use.</p></div><button className="primary-button" onClick={() => notify('Upload area ready — select a document to begin')}><Upload size={16} />Upload document</button></div><div className="upload-zone" onClick={() => notify('Mock upload started')}><div className="upload-icon"><Upload size={20} /></div><div><b>Drop compliance documents here</b><p>or <u>browse files</u> · PDF, DOCX up to 50 MB</p></div></div><section className="panel documents-panel"><div className="panel-header"><div><h2>Document library <span className="count-badge">12</span></h2><p>All approved and uploaded source material</p></div><div className="table-tools"><div className="search-box"><Search size={15} /><input aria-label="Search documents" placeholder="Search documents" /></div><button className="select-button">All status <ChevronDown size={14} /></button></div></div><div className="document-table"><div className="table-row table-head"><span>Document name</span><span>Pages</span><span>Status</span><span>Last indexed</span><span /></div>{documents.map((doc) => <button className="table-row document-row" key={doc.name} onClick={() => setEvidence(true)}><span className="doc-name"><span className="file-icon"><FileText size={17} /></span><span><b>{doc.name}</b><small>{doc.type} document</small></span></span><span>{doc.pages}</span><span><StatusPill tone={doc.tone as 'success' | 'warning' | 'danger'}>{doc.status}</StatusPill></span><span className="muted-text">{doc.date}</span><MoreHorizontal size={17} /></button>)}</div></section></div> }

function Assistant({ setEvidence, notify }: { setEvidence: (open: boolean) => void; notify: (message: string) => void }) { const [question, setQuestion] = useState(''); const [asked, setAsked] = useState(false); const ask = () => { if (!question.trim()) return; setAsked(true); notify('Question evaluated against approved sources'); setQuestion('') }; return <div className="page-content assistant-page"><div className="page-heading"><div><div className="eyebrow"><span className="eyebrow-line" />GROUNDED CHAT</div><h1>Compliance Assistant</h1><p>Answers are grounded in your approved compliance documents.</p></div><StatusPill>Evidence-only mode active</StatusPill></div><div className="assistant-layout"><section className="panel chat-panel"><div className="chat-head"><div className="assistant-orb"><ShieldCheck size={19} /></div><div><b>CompliSure Assistant</b><small>Approved sources only · Ready</small></div><button className="icon-button"><MoreHorizontal size={18} /></button></div><div className="messages"><div className="message user-message"><span className="chat-avatar user">AM</span><div><small>You</small><p>What is the maximum travel reimbursement allowed?</p></div></div><div className="message assistant-message"><span className="chat-avatar bot"><ShieldCheck size={15} /></span><div className="message-body"><small>CompliSure</small><p>The maximum travel reimbursement allowed is <strong>₹5,000 per trip.</strong></p><div className="evidence-card"><div className="evidence-card-top"><span className="evidence-label"><CheckCircle2 size={15} />Evidence found</span><span className="confidence high">Confidence: High</span></div><p>Strong supporting evidence</p><div className="source-row"><FileText size={15} /><span><b>Travel Policy.pdf</b><small>Page 7 · Travel Reimbursement</small></span><button onClick={() => setEvidence(true)}>View source <ChevronRight size={14} /></button></div></div></div></div><div className="message user-message"><span className="chat-avatar user">AM</span><div><small>You</small><p>What is the reimbursement limit for international business-class travel?</p></div></div><div className="refusal-message"><div className="refusal-title"><span className="refusal-icon"><AlertTriangle size={17} /></span><div><b>Answer withheld</b><small>Insufficient evidence detected</small></div><span className="confidence low">Confidence: Low</span></div><p>I can&apos;t answer this reliably from the provided compliance documents.</p><div className="refusal-note"><AlertTriangle size={14} /><span><b>No supporting evidence found</b><small>Answer withheld to prevent an unsupported or confident guess.</small></span></div><div className="refusal-actions"><button className="secondary-button" onClick={() => notify('No relevant evidence was retrieved')}>View retrieved evidence</button><button className="primary-button" onClick={() => setQuestion('')}>Ask another question</button></div></div>{asked && <div className="message assistant-message"><span className="chat-avatar bot"><ShieldCheck size={15} /></span><div className="message-body"><small>CompliSure</small><p>I&apos;ll evaluate that question against the approved compliance sources and only answer if sufficient evidence is found.</p><div className="limited-note"><Gauge size={15} /> Mock evaluation complete · Evidence status: <b>Review required</b></div></div></div>}</div><div className="suggestions"><span>Try asking</span><button onClick={() => setQuestion('What expenses require pre-approval?')}>What expenses require pre-approval?</button><button onClick={() => setQuestion('How long are incident records retained?')}>How long are incident records retained?</button></div><div className="composer"><button className="attach-button" aria-label="Attach document"><Paperclip size={18} /></button><input value={question} onChange={(e) => setQuestion(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.nativeEvent.isComposing && e.keyCode !== 229) ask() }} placeholder="Ask a compliance question..." /><button className="send-button" onClick={ask} aria-label="Send question"><Send size={17} /></button></div><div className="composer-foot"><span><ShieldCheck size={13} />Only approved documents are used</span><span>Enter to send</span></div></section><aside className="assistant-side"><div className="side-card"><div className="side-card-icon green"><CheckCircle2 size={17} /></div><b>Evidence available</b><p>Answers include the exact source, page, and section used.</p></div><div className="side-card"><div className="side-card-icon orange"><AlertTriangle size={17} /></div><b>Evidence unavailable</b><p>The assistant refuses instead of filling gaps with a guess.</p></div><div className="coverage-card"><div className="coverage-top"><span>Source coverage</span><b>87%</b></div><div className="progress"><span style={{ width: '87%' }} /></div><small>Across your approved document set</small></div></aside></div></div> }

function Evaluations() { return <div className="page-content"><div className="page-heading"><div><div className="eyebrow"><span className="eyebrow-line" />MEASURE SAFETY</div><h1>Evaluation &amp; Safety Testing</h1><p>Measure how effectively safeguards prevent confident wrong answers.</p></div><button className="primary-button"><Plus size={16} />New evaluation</button></div><div className="eval-kpis"><StatCard label="Total test questions" value="250" change="Current cycle" icon={ClipboardCheck} tone="blue" /><StatCard label="Answerable questions" value="218" change="87.2% of total" icon={CheckCircle2} tone="green" /><StatCard label="Unanswerable questions" value="32" change="12.8% of total" icon={CircleHelp} tone="orange" /><StatCard label="Confident wrong answers" value="8" change="↓ 42% from baseline" icon={AlertTriangle} tone="orange" /><StatCard label="Correct refusals" value="28" change="87.5% refusal rate" icon={ShieldCheck} tone="purple" /></div><div className="eval-grid"><section className="panel evaluation-table-panel"><div className="panel-header"><div><h2>Safeguard comparison</h2><p>How each configuration performs on the same test set</p></div><button className="select-button">This cycle <ChevronDown size={14} /></button></div><div className="eval-table"><div className="eval-row eval-head"><span>Method</span><span>Correct answers</span><span>Wrong answers</span><span>Correct refusals</span><span>Wrong answer rate</span></div>{evalRows.map((row) => <div className="eval-row" key={row[0]}><span><b>{row[0]}</b></span><span className="good-text">{row[1]}</span><span className={row[4] === 'High risk' ? 'bad-text' : 'warning-text'}>{row[2]}</span><span>{row[3]}</span><span><StatusPill tone={row[4] === 'High risk' ? 'danger' : row[4] === 'Strongest' ? 'success' : 'warning'}>{row[4]}</StatusPill></span></div>)}</div></section><section className="panel metrics-panel"><div className="panel-header"><div><h2>Safety metrics</h2><p>Current cycle performance</p></div></div><div className="metric-ring-wrap"><div className="metric-ring"><div><b>98.4%</b><small>safe answers</small></div></div></div><div className="metric-list"><div><span><i className="metric-dot green" />Answer accuracy</span><b>87.2%</b></div><div><span><i className="metric-dot orange" />Correct refusal rate</span><b>87.5%</b></div><div><span><i className="metric-dot red" />Wrong-answer rate</span><b>1.6%</b></div></div></section></div><section className="panel test-cases"><div className="panel-header"><div><h2>Test case breakdown</h2><p>Representative questions from the evaluation set</p></div><button className="text-button">View all 250 <ChevronRight size={14} /></button></div><div className="case-list">{[['What is the international travel reimbursement limit?', 'Refuse', 'Refused', 'PASS', 'success'], ['Can employees share credentials with contractors?', 'Answer with source', 'Answered with source', 'PASS', 'success'], ['What is the retention period for visitor logs?', 'Answer with source', 'Answered with limited evidence', 'WARNING', 'warning']].map((item) => <div className="case-row" key={item[0]}><div><b>{item[0]}</b><small>Expected: {item[1]}</small></div><span>{item[2]}</span><StatusPill tone={item[4] as 'success' | 'warning'}>{item[3]}</StatusPill></div>)}</div></section></div> }

function Safeguards() { const cards = [{ icon: Search, name: 'Evidence Retrieval', desc: 'Retrieve relevant passages from approved documents.', config: 'Top 5 passages · Hybrid search' }, { icon: Gauge, name: 'Evidence Threshold', desc: 'Refuse when retrieved evidence is not sufficiently relevant.', config: 'Minimum relevance · 82%' }, { icon: ClipboardCheck, name: 'Source Verification', desc: 'Verify that generated answers are supported by retrieved evidence.', config: 'Citation required · Exact match' }, { icon: ShieldCheck, name: 'Safe Refusal', desc: 'Do not provide an answer when sufficient evidence is unavailable.', config: 'Unsupported answers · Blocked' }]; return <div className="page-content"><div className="page-heading"><div><div className="eyebrow"><span className="eyebrow-line" />CONTROL CENTER</div><h1>Safeguards</h1><p>Configure the mechanisms that keep every answer grounded and accountable.</p></div><StatusPill>4 of 4 active</StatusPill></div><div className="safeguard-hero"><div><div className="hero-icon"><ShieldCheck size={22} /></div><div><b>Safety layer is protecting your assistant</b><p>All safeguards are active. Unsupported answers are currently blocked.</p></div></div><div className="hero-stat"><strong>4/4</strong><span>Active safeguards</span></div></div><div className="safeguard-cards">{cards.map(({ icon: Icon, name, desc, config }) => <section className="panel safeguard-card" key={name}><div className="safeguard-card-top"><span className="safeguard-card-icon"><Icon size={18} /></span><StatusPill>Active</StatusPill></div><h2>{name}</h2><p>{desc}</p><div className="config"><small>Configuration</small><b>{config}</b></div><button className="text-button">View configuration <ChevronRight size={14} /></button></section>)}</div></div> }

function SettingsPage() { return <div className="page-content"><div className="page-heading"><div><div className="eyebrow"><span className="eyebrow-line" />WORKSPACE</div><h1>Settings</h1><p>Manage assistant behavior and approved document preferences.</p></div><button className="primary-button" onClick={() => alert('Settings saved in mock mode')}>Save changes</button></div><div className="settings-grid"><section className="panel settings-section"><div className="settings-heading"><div className="settings-icon"><ShieldCheck size={18} /></div><div><h2>AI Safety</h2><p>Control how the assistant handles uncertainty.</p></div></div><div className="setting-row"><div><b>Evidence threshold</b><small>Minimum relevance score required to answer</small></div><div className="range-wrap"><input type="range" defaultValue="82" /><b>82%</b></div></div><div className="setting-row"><div><b>Require source citation</b><small>Include document and page details with every answer</small></div><div className="toggle on"><span /></div></div><div className="setting-row"><div><b>Enable answer verification</b><small>Check that generated claims are supported by evidence</small></div><div className="toggle on"><span /></div></div><div className="setting-row"><div><b>Allow unsupported answers</b><small>Permit the assistant to answer without retrieved evidence</small></div><div className="setting-off">OFF</div></div></section><section className="panel settings-section"><div className="settings-heading"><div className="settings-icon"><FileText size={18} /></div><div><h2>Document settings</h2><p>Manage indexing behavior for your knowledge base.</p></div></div><div className="setting-row"><div><b>Automatic indexing</b><small>Index approved documents when they are uploaded</small></div><div className="toggle on"><span /></div></div><div className="setting-row"><div><b>Supported file types</b><small>Accepted formats for compliance sources</small></div><b className="format-list">PDF · DOCX</b></div></section></div></div> }

function EvidencePanel({ onClose }: { onClose: () => void }) { return <div className="evidence-overlay" onClick={onClose}><aside className="evidence-panel" onClick={(e) => e.stopPropagation()}><div className="evidence-panel-head"><div><div className="eyebrow"><span className="eyebrow-line" />SOURCE EVIDENCE</div><h2>Travel Policy.pdf</h2></div><button className="icon-button" onClick={onClose} aria-label="Close evidence panel"><X size={18} /></button></div><div className="source-meta"><div><small>Page</small><b>7 of 18</b></div><div><small>Section</small><b>Travel Reimbursement</b></div><div><small>Relevance</small><b className="relevance">94%</b></div></div><div className="document-preview"><div className="preview-top"><span>EMPLOYEE TRAVEL POLICY</span><span>7</span></div><h3>4. Travel Reimbursement</h3><p>Employees may claim reasonable travel expenses incurred while conducting approved business on behalf of the University.</p><p className="highlighted">Employees may claim travel reimbursement <mark>up to ₹5,000 per trip</mark> for domestic travel. Claims must be submitted within 30 days of the travel date.</p><p>Any exception to this limit requires prior written approval from the Finance Office.</p></div><div className="evidence-note"><CheckCircle2 size={17} /><div><b>Evidence supports the answer</b><p>The highlighted text directly supports the reimbursement amount provided.</p></div></div><button className="primary-button full-button" onClick={onClose}>Done</button></aside></div> }

export default function Page() { const [active, setActive] = useState<Section>('Dashboard'); const [evidence, setEvidence] = useState(false); const [toast, setToast] = useState(''); const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }; return <div className="app-shell"><Sidebar active={active} setActive={setActive} /><main className="main-area"><Topbar active={active} onMenu={() => notify('Use the navigation to switch sections')} />{active === 'Dashboard' && <Dashboard />}{active === 'Documents' && <Documents notify={notify} setEvidence={setEvidence} />}{active === 'AI Assistant' && <Assistant setEvidence={setEvidence} notify={notify} />}{active === 'Evaluations' && <Evaluations />}{active === 'Safeguards' && <Safeguards />}{active === 'Settings' && <SettingsPage />}</main>{evidence && <EvidencePanel onClose={() => setEvidence(false)} />}{toast && <div className="toast"><Check size={15} />{toast}</div>}</div> }
